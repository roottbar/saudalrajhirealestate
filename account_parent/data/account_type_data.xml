<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- account.account.type model no longer exists in Odoo 15+ -->
        <!-- This data is disabled for compatibility -->
        <!-- The is_view field on account.account is now used instead -->
        <!--
        <record model="account.account.type" id="data_account_type_view">
            <field name="name">View</field>
            <field name="type">view</field>
            <field name="internal_group">off_balance</field>
            <field name="note">Used only for parent accounts</field>
            <field name="include_initial_balance" eval="False" />
        </record>
        -->
    </data>
</odoo>
