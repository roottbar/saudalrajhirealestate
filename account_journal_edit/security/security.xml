<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- مجموعة الأمان -->
        <record id="group_journal_edit_after_confirm" model="res.groups">
            <field name="name">يمكن تعديل اليومية بعد التأكيد</field>
            <field name="category_id" ref="base.module_category_accounting"/>
            <field name="implied_ids" eval="[(4, ref('account.group_account_user'))]"/>
        </record>

        <!-- قاعدة الأمان -->
        <record id="account_move_edit_after_confirm_rule" model="ir.rule">
            <field name="name">تعديل اليومية بعد التأكيد</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('account_journal_edit.group_journal_edit_after_confirm'))]"/>
            <field name="perm_read" eval="1"/>
            <field name="perm_write" eval="1"/>
            <field name="perm_create" eval="1"/>
            <field name="perm_unlink" eval="0"/>
        </record>
    </data>
</odoo>